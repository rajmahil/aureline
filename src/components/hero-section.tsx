import React from "react";

const HeroSection = () => {
  return (
    <section className="p-10 h-screen">
      <div className=" w-full bg-red-200 h-full">ddd</div>
    </section>
  );
};

export default HeroSection;
